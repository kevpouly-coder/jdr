# Cible macOS

Activer le support macOS et générer les fichiers :

```bash
flutter config --enable-macos-desktop
flutter create --platforms=macos .
```

Les fichiers Xcode peuvent ensuite être ouverts pour configurer les identifiants de signature.
