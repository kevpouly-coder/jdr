# Cible Linux

Exécuter les commandes suivantes pour activer et générer les artefacts Linux :

```bash
flutter config --enable-linux-desktop
flutter create --platforms=linux .
```

Les fichiers générés peuvent être personnalisés pour intégrer des dépendances natives spécifiques.
