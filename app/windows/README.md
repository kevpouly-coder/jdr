# Cible Windows

Activer le support Windows et générer les fichiers nécessaires :

```powershell
flutter config --enable-windows-desktop
flutter create --platforms=windows .
```

Vous pouvez ensuite ouvrir le dossier `windows` dans Visual Studio pour ajuster les dépendances natives.
